[project]
name = "subtitle-fts"
version = "0.1.0"
description = "Full-text search indexer for SRT subtitle files using MeiliSearch"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
